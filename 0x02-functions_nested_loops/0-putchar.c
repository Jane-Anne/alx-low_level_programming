#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * Return: Always zero (Successful)
 */

int main(void)
{
	char ch = 'p';
//write the character to stdout
	putchar(ch);

	return (0);
}
